const DELAY = 1500; // delay to show data
const MAXSVGWIDTH = 170; // svg container width
const ROWHEIGHT = 20; //row height in pixels
const XAXISHEIGHT = 25; // x axis legend size
const BARDATAXOFFSET = 10; // bar row x offset to show the value
const BARDATAYOFFSET = 5; // bar row y offset to show the value
const FALLBACKCOLOR = '#000000'; // theme provider color fallback value
const FALLBACKFONT = 'Courier'; // theme provider font fallback value

export {
  DELAY,
  MAXSVGWIDTH,
  ROWHEIGHT,
  BARDATAXOFFSET,
  BARDATAYOFFSET,
  FALLBACKCOLOR,
  FALLBACKFONT,
  XAXISHEIGHT,
};
